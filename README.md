# Project Name

> One-line description of what this project does.

## Setup

```bash
cp .env.example .env
# fill in your values

pip install -r requirements.txt
```

## Usage

```bash
python src/main.py
```

## Structure

```
src/        # main scripts
scripts/    # run/deploy helpers
tests/      # tests
output/     # generated artifacts (gitignored)
docs/       # architecture, decisions, testing notes
tasks/      # active task context for Claude
```

## Notes
- See `CLAUDE.md` for AI assistant context
- See `docs/decisions.md` for why things are built the way they are
